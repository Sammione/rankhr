services:
  - type: web
    name: rank-ai-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 --timeout 600 -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
